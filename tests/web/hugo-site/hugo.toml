# The web tests' Hugo site (tests/web/web.ts builds it, served under /hugo/).
baseURL = "http://example.org/hugo/"
title = "Reflow TeX – Hugo fixture"
disableKinds = ["taxonomy", "term", "RSS", "sitemap"]
