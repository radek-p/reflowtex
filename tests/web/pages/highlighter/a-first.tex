A prime is a number with exactly two divisors. \webid{key}{Every number
above one is a product of primes, in only one way}, and that is why they
are called the atoms of arithmetic.\footnote{Euclid knew the first half.}
The second sentence goes on long enough to be broken into several lines at
any width the tests use, so that a highlight can reach across a line break
and keep its band on every line it is drawn on.
