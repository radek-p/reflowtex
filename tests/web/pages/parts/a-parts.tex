A button \webwidget[tone=warm, default={(see note)}]{popover}\webpart{label}{more}\webpart{note}{The note
behind the button, typeset out of the flow.} stands in this line, and
\webid{key}{the key sentence of the paragraph} is named, while
\webclass{hot}{this part is classed \webid{inner}{and this named inside it}}.
A side note follows.\note[place=margin, n=1]{Beside its line.}
\begin{webstream}[variant=card]{box}
\webpart{summary}{A summary of the box.}
The box's own text, which is long enough to be a paragraph of its own.
\end{webstream}
\begin{warn}[variant=accent, --rtx-accent=#c2410c]
Dividing by $x$ assumes $x \neq 0$.
\end{warn}
