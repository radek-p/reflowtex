A prime is a number with exactly two divisors. \webid{key}{Every number
above one is a product of primes, in only one way}, and that is why they
are called the atoms of arithmetic. The second sentence goes on long
enough to be broken into several lines at any width the tests use, so that
a selection reaches across line breaks and has a band on every line.
