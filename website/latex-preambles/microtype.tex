% SPDX-License-Identifier: AGPL-3.0-or-later
% Character protrusion and font expansion, which the viewer reproduces when
% it re-breaks: appended to testmath.tex's own preamble on the Showcase page,
% and the whole preamble of a block that needs nothing else (the hero, the
% inspector's demo). Every other preamble of the site loads it too.
\usepackage{microtype}
